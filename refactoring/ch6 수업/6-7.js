// 통상적으로 width를 먼저 쓴다.
let area = width * height;

// cpy는 copy로도 많이쓰이기 때문에 이름이 뭘 뜻하는지 알기 힘들다.
// 한참을 생각해서 유추해야하는 이름이라면 구체적으로 이름을 적어주는 것이 더 좋다.
const companyName = "애플";

// tbHd는 무엇을 뜻하는지 알기 어렵다. 명확한 뜻을 가진 단어로 바꿔주자.
let title = "제목없음";
let result = `<h1>${title}</h1>`;
